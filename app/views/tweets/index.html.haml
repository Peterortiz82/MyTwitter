%h1 Tweets
- if @tweets.present?
  %table
    %tbody
      %tr
        %td User:
        %td Tweet:
        %td Posted on:
      - @tweets.each do |t|
        %tr
          %td= t.user.name
          %td= t.tweet_body
          %td= t.created_at.to_formatted_s(:long)
- else
  %p Post your first tweet!
  = link_to 'Tweet', new_tweet_path, class: 'btn btn-default'